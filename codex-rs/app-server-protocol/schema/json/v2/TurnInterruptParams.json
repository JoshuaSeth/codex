{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
    "respondImmediately": {
      "description": "When true, respond as soon as the interrupt request is accepted. The client must still wait for `turn/completed` to observe final cleanup.",
      "type": "boolean"
    },
    "threadId": {
      "type": "string"
    },
    "turnId": {
      "type": "string"
    }
  },
  "required": [
    "threadId",
    "turnId"
  ],
  "title": "TurnInterruptParams",
  "type": "object"
}